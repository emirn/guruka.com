---
import type { SiteConfig } from '../lib/config';

interface Props {
  config: SiteConfig;
}

const { config } = Astro.props;

// Show banner whenever robots_no_index is true (controlled by project-level preview flag)
const showBanner = config.seo.robots_no_index === true;
const bannerText = config.seo.preview_banner_text || 'Preview Mode - This site is not indexed by search engines';
---

{showBanner && (
  <div class="bg-red-600 text-white text-center py-2 px-4 text-sm font-medium">
    <div class="max-w-7xl mx-auto flex items-center justify-center gap-2">
      <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
      </svg>
      <span>{bannerText}</span>
    </div>
  </div>
)}
